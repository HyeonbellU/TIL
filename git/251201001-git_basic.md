# Git basic

## What is git?

git은 리눅스 개발자가 제작한 버젼관리 툴

$ : interaction이 준비되었다.
지금 들어오는 이용자별로 접근 가능한 값이 다륻ㅏ.
~ : user folder : 로그인 유저에게는 특별한 권한 없이 모든 행ㄷㅇ 가능

/ : 하드디스크의 최상단 폴더

유저명@컴퓨터 이름

ls : 모든 리스트들 보여준다. (cli는 내가 보면서 확인해야 한다.=>탐색기는 필요없는 커맨드.

개발할 때는 documents 쪽으로 몰아넣어라.

main 명령어
option 명령어(flag)

ls -a :
. 이 붙어있으면 unix like system의 숨김파일 표시 방식
-a : all files
-l : 파일의 세부정보,  권한설정을 확인하는 방법
-al : all files line by line

~ : 현재 작업 중이다
pwd : print working directory, 현재 작업 위치

CLI는 대소문자 구분한다.
이동 : cd '목적지'
change directory
$ 왼쪽에 대략적인 현재 작업 위치가 나온다.

디렉토리 만들기
mikdr (make directory) : 디렉토리는 실제 물리량 없고 그냥 구분태그

내려가는 법 : cd '해당디렉토리명'
올라가는 법 : cd ..

github : 백업+소스코드 관리


새 파일 만들기 : touch '파일명.확장자'
특수 프로그램을 통해서 실행되는 파일들은 touch로 생성시켰을 때 제대로 실행되는지는 미지수(해당 프로그램들ㅇㅔ서 새 파일을 킬 때 초기화 과정을 거치기 때문ㅇㅔ)

텍스트 기반 구동 파일만 이렇게 형성해야 한다.

파일 이동 : mv '파일' '목적지'
ls '하위 디렉토리' : 확인

와일드카드
: 쉬프트팔 에스??리슼ㅡ??
ex)
.py로 끝나는 모든 파일 옮기기
=> mv *.py bin
m으로 시작ㅎㅏ는 모든 파일 옮기기
=> mv m*.* bin

임베디드 : 특정 기능을 위한 것
git bash : 임베디드 쉘, 모든 기능을 제공하지 않는다.
ㄴ 환경변수를 모두 실행해도 오류가 작동 가능
ㄴ 터미널보다는 떨어진다.
ㄴ 윈도우에서 git을 쓰도록 해주는 것
ㄴ VSterminal이나 파워쉘에서 ㄱㄱ

복사
현재 위치에 복사
cp index.html ./index_copy.html [.이 현재 위치, .. 이 상위 폴더]
'_copy' 안 붙이면 오류 발생

이름 바꾸기
파일의 위치 바꾸기==파일의 이름 바꾸기
mv '파일명.확장자' ./새파일이름

=> 옮기면서 이름도 바꾸기 가능
mv '파일명' ./옮기는디렉토리/새파일이름

파일 삭제
cf) 삭제와 제거는 다르다.
remove : 가장 일반적 제거, 논리적 삭제
erase : 오브젝트 단위가 아닌 물리적인 디스크 단위 삭제
delete : 진짜 삭제, 포멧 수준

할당된 번지를 없애는 거다. 진짜 없어지는게 아니다.
딜리트는 파일도 없앤다.rkq

rm 파일명
디렉토리는 이걸로 삭제 안된다.
안에 파일에 있어서...
rm -r 해당 디렉토리
ㄴ 하위 파일ㅇㅡㄹ 모두 지우고 나와서 해당 디렉토리를 지워라(나무 다 깎고 산 깎기)

rm / 하면 큰일난다..
다 날려먹는다.


Vim
vi를 확장(개선)시킨 툴
CLI
커밍메세지 확인용으로 사용할 것.
거의 대부분 내장된 툴(윈도우 파워셀, cmd 제외..)


마크다운 : 현대적인 컴퓨터를 하면서 알아야할 텍스트 포멧
LLM 모두 데이터에 대한 학습을 마크다운 또는 json 구조화하여 학습한다.
질문을 던질 때도 마크다운이나 json 기반으로 작업해서 던지면 더 빠르고 정확하게 안다.

markdown == md
vim은 한글 입력시 모두 장애 발생.

vi learn-md.md 실행함ㅕㄴ 까만 화면
vi로 .md로 실행시킨다.
[어떤 프로그램] [ 활용할 프로그램]



파일을 수정하기 위해선 노멀모드에서 인서트 모드
i를 입력한다.

# : 헤딩 텍스트
<h1></h1>과 동일 : html은 markdown에서 렌더링 가능
## == h2   -> 6단계 모두 사용 가능, 건너뛰기 불가능
##'띄워쓰기'쓰기, -'띄워쓰기'

어떤 모드든 esc를 누르면 노멀모드로 간다.
-: unordered list, 순서 없다.
1.	/2. /3.  순서 있다.
일반적 문단 구분은 엔터를 쳐야 한다.
엔터를 한번만 치면 뒤에 붇는다. 확실히 한 줄 띄우ㅓ서 엔터를 쳐준다.

옵시디언

커멘드 모드에서 ':n'으로 해당 n line으로 바로 간다.
:w   저장
ㄴ nnL, nnnB written
:q 나가기
:wq 저장후 나가기
:q! 
read only 파일은 
:w!

catinate
cat '파일명' : 파일속 모든 텍스트를 쉘에서 보여줘.

gitlab
사설서버의 구축이 가능하다.

다른 서버와 통신 : bitbucket


git clone?
remote repo를 만들어서 pc로 가져오기(톱 다운 방식)

라이센스 잘 확읺기 GNU GLP 절대 쓰지말자

새 파일 touch!!
무언가 작업을 하고 계속 ls로 확인을 해주자!!

untracked file 지금ㄲㅏ지 git이 본적 없는 파일
=> 등록해줘야함.
=> git add '팡리명"

git commit 하면 vim 이 뜬다.
= commit message를 입력한다.	
i 누르기
메세지 입력
esc
:wq 엔터

[main "commit id"]
모든 커밋은 해쉬값을 가지고 있ㅇㅓ 앞 7자릴ㅡㄹ 가져와서 넣는다.

[README.md]
logo
description 1line
bedge
description
install guide
coverage

=> 내 git의 대문

# project name
abstract, Demo link
badges
기능들
팀 내부면 테스트 방법
credit

파일이름 대충 4~5글자 치고 탭으로 자동ㅇㅘㄴ성

리드미는 documentation용 : 전반적 설명 자체
main은 실제 프로젝트 작동의 logic이 있다. :
ㄴ 따라서 따로 커밋을 올려야 한다.
작업의 단위를 고민하는 것...git은 결국 버젼관리(history)를 다룩ㅣ 때문에 대원칙이 존재한다. 작업단위에 따라 commit 을 구분한다.

commit을 읽을 때, 쓰는 방법을 봐야 한다. log를 얼마나 일관성있게 남기는지 본다.
구나 절의 형태, 띄어쓰기, 대묹ㅏ로 구분
prefix 꼭달기

commit다느ㄴ건 시간순서대로 하는 것을 추천

 push는 너무 잦은 빈도로 하지 말자. 수정이 필요할 때 골ㅊㅣ아파진다. push안했을 때 수정이 쉽ㄷ.

운ㅇㅕㅇ체계, 언어, 프레임워크를 통해 미리 .gitignore를 생성

CICD?


ipynb
=> interactive python : 셀 단위로 보게 한다.

pre-commit
커밋 전 한번 기계적인 확인해주는 것.
isort, autopep8, flack4 해보자(파이썬)



*가 현재 있는 branch를 말한다.
bash에서 초록색 (main)도 같은 브랜치를 말하는거다.
git branch -r (옮겨가기 remote)
git branch -v
origin -> 소스콛ㅡ
HEAD : 최신, 마지막, 현재(commit, branch에 있어서)
git branch -a : all branches

git branch 'new branch name'

loop 브랜치로 옮겨가기
git swith 'branch name'

vim에서는 스페이스와 탭을 섞어쓰면 안된다.
git merge 'branch name'
Fast-forward 발생
=> main으로 데려온다.(반영이 된다.)
=> 물리적으로 병합되는건 아니다.

